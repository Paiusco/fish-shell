# fish completion for bitbake


complete -c bitbake -l version -d "Show Bitbake's version number and exit"
complete -c bitbake -s h -l help -d "Show this help message and exit"
complete -c bitbake -s k -l continue -d "Continue as much as possible after an error.
